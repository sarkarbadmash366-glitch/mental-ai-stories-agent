import './globals.css';

export const metadata = {
  title: 'Mental AI Stories — Viral Agent',
  description: 'AI manager for viral vegetable stories'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
